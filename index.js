const printHomeWorkStatus = () => {
  const stringSymbols = "*".repeat(5);
  const mainMessage = "Homework done!";
  console.log(`${stringSymbols} ${mainMessage} ${stringSymbols}`);
};

printHomeWorkStatus();
